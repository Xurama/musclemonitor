export type MuscleGroupTypeDb = {
  muscle_group_type_id: number; // Identifiant unique du type de groupe musculaire
  name: string; // Nom du type de groupe musculaire (par exemple "pectoraux", "dos", etc.)
};
